apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: kubernetes-lab
data:
  PORT: "8080"

  REDIS_HOST: "redis-service"
  REDIS_PORT: "6379"

  POSTGRES_HOST: "postgres-service"
  POSTGRES_PORT: "5432"
  POSTGRES_DB: "appdb"
